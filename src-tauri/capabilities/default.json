{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Only bundled main-window code may invoke registered Context Vault commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
